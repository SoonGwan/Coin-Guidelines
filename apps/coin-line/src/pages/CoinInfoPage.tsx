import React from "react";
import PageTemplate from "../components/PageTemplate";
import CoinInfoContainer from "../container/CoinInfoContainer";

const CoinInfoPage = () => {
	return (
		<PageTemplate>
			<CoinInfoContainer />
		</PageTemplate>
	);
};

export default CoinInfoPage;
